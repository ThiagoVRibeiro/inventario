package com.inventario.repositories;

import org.springframework.data.jpa.repository.JpaRepository;

import com.inventario.models.ProcessadorModel;

public interface ProcessadorRepository extends JpaRepository<ProcessadorModel, Integer>{

}
